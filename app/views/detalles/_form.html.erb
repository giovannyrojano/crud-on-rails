<%= form_with(model: detalle) do |form| %>
  <% if detalle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(detalle.errors.count, "error") %> prohibited this detalle from being saved:</h2>

      <ul>
        <% detalle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad %>
  </div>

  <div class="field">
    <%= form.label :Prestamo_id %>
    <%= form.text_field :Prestamo_id %>
  </div>

  <div class="field">
    <%= form.label :libro_id %>
    <%= form.text_field :libro_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
