<h1>New Infraccion</h1>

<%= render 'form', infraccion: @infraccion %>
<div class="text-center">
<%= link_to  infraccions_path ,style:'color: var(--orange)'  do %>
    <i class="fas fa-caret-square-left fa-3x"></i>
    <% end %>
    </div>
